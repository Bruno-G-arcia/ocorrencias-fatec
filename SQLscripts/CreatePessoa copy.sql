USE OCORRENCIAS;

CREATE TABLE Cliente(
  IdUsuario     INTEGER AUTO_INCREMENT NOT NULL,
  Email          VARCHAR(100),
  Senha          VARCHAR(100),
  Tipo           VARCHAR(100),
  PRIMARY KEY(IdEndereco)
);