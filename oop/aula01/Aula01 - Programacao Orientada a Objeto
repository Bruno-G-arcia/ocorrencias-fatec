Aula01 - Programacao Orientada a Objeto
    Diagrama de classes.
    Entidades abstratas

    Objeto, classe, atributos

joao = new Pessoa();

diagrama de classe -> + significa publico
                      - significa privado
                      # protegido

Class -> CS 
Modificadores de Acesso ->
    Internal
    .cs -> compilador SDK -> MSIL -> (.dll)
    runtime interpreta o .dll (CLR)


A(internal) -> B(public) : Possuem dll diferentes

using p = Pessoa -> alias

struct (int) / class (string)

C.S. -> todos elementos são objetos

MVC -> model view controller

TRY CATCH EXCEPTION


Para próxima aula criar classe Produto->
    incluir atributos que representam produto.
    preço
    descricao
    quantidade
    validade
    
