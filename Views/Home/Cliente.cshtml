@using OcorrenciasWeb.Models
@using System.Security.Claims;
@{
  Layout = "/Views/Layout.cshtml";
}

 <div class="HomeContainer">
    <div class="HomeTitle"> Bem vindo @ViewBag.ClNome !</div>

    <h3>Minhas Ocorrencias:</h3>
    <table class="table table-hover">
      <tr>
        <th>Ocorrencia</th>
        <th>Titulo</th>
        <th>Abertura</th>
        <th>Prazo</th>
        <th>Status</th>
        <th></th>
      </tr>
      @if (Model != null)
      {
        @foreach (Ocorrencia o in Model)
        {

          <tr>
            <td>@o.IdOcorrencia</td>
            <td>@o.Titulo</td>
            <td>@o.Abertura.ToString("dd/MM/yyyy")</td>
            <td>@o.Prazo.ToString("dd/MM/yyyy")</td>
            <td>@o.Status</td>
            <td>
              <a href="/OrdemServico/CDetalhes?idOcorrencia=@o.IdOcorrencia">Detalhes</a>
            </td>
          </tr>

        }
      }

    </table>

    <a href="/Ocorrencia/Create" class="btn btn-primary">Criar Nova Ocorrencia</a>
    </div>


